version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "ecs-cicd-taskdef2"
        LoadBalancerInfo:
          ContainerName: "web"
          ContainerPort: 80
